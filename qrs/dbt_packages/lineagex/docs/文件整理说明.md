# Lineagex 文件整理说明

## 📋 整理概述

为了让 lineagex 项目结构更清晰、更易维护，我们对文件进行了重新组织。

**整理日期**: 2026-01-04  
**整理原则**: 按功能分类，清晰的目录结构

---

## 📁 新的目录结构

```
lineagex/
├── README.md                    # 项目主文档（新建）
├── requirements.txt             # Python 依赖
├── .gitignore                   # Git 忽略规则（新建）
│
├── 核心模块/（根目录）
│   ├── main.py                  # 主入口
│   ├── lineage.py               # 血缘分析核心逻辑
│   ├── column_lineage.py        # 列级血缘分析
│   ├── db_connector.py          # 数据库连接器
│   └── utils.py                 # 工具函数
│
├── docs/                        # 📚 文档目录（新建）
│   ├── 血缘分析使用指南.md       # 中文使用指南
│   ├── OUTPUT_JSON_GUIDE.md     # output.json 详细说明
│   ├── MIGRATION_SUMMARY.md     # 迁移总结
│   ├── FIX_REPORT.md            # 修复报告
│   ├── TEST_REPORT.md           # 测试报告
│   ├── ORIGINAL_README.md       # 原始 README（保留）
│   └── 文件整理说明.md          # 本文档
│
├── tests/                       # 🧪 测试目录（新建）
│   ├── run_all_tests.sh         # 运行所有测试
│   ├── test_db_connector.py     # 数据库连接测试
│   ├── test_imports.py          # 模块导入测试
│   ├── test_lineage_integration.py  # 集成测试
│   └── test_error_handling.py   # 错误处理测试
│
├── examples/                    # 💡 示例目录（新建）
│   ├── example_lineage_analysis.py  # 分析示例
│   └── visualize_lineage.py     # 可视化示例
│
├── output/                      # 📊 输出目录（新建）
│   ├── output.json              # 血缘数据（生成）
│   └── index.html               # 可视化页面（生成）
│
├── 前端资源/（根目录）
│   ├── app.js                   # 前端应用
│   ├── vendor.js                # 第三方库
│   └── example.png              # 示例截图
│
└── integration_tests/           # dbt 集成测试（保留）
    └── ...
```

---

## 🔄 文件移动记录

### 文档文件 → `docs/`

| 原位置 | 新位置 | 说明 |
|--------|--------|------|
| `FIX_REPORT.md` | `docs/FIX_REPORT.md` | JSON 解析修复报告 |
| `MIGRATION_SUMMARY.md` | `docs/MIGRATION_SUMMARY.md` | 迁移总结 |
| `OUTPUT_JSON_GUIDE.md` | `docs/OUTPUT_JSON_GUIDE.md` | output.json 说明 |
| `TEST_REPORT.md` | `docs/TEST_REPORT.md` | 测试报告 |
| `血缘分析使用指南.md` | `docs/血缘分析使用指南.md` | 中文使用指南 |
| `README.md` | `docs/ORIGINAL_README.md` | 原始 README（保留） |

### 测试文件 → `tests/`

| 原位置 | 新位置 | 说明 |
|--------|--------|------|
| `test_db_connector.py` | `tests/test_db_connector.py` | 数据库连接测试 |
| `test_imports.py` | `tests/test_imports.py` | 模块导入测试 |
| `test_lineage_integration.py` | `tests/test_lineage_integration.py` | 集成测试 |
| `test_error_handling.py` | `tests/test_error_handling.py` | 错误处理测试 |
| `run_all_tests.sh` | `tests/run_all_tests.sh` | 测试运行脚本 |

### 示例文件 → `examples/`

| 原位置 | 新位置 | 说明 |
|--------|--------|------|
| `example_lineage_analysis.py` | `examples/example_lineage_analysis.py` | 分析示例 |
| `visualize_lineage.py` | `examples/visualize_lineage.py` | 可视化示例 |

### 输出文件 → `output/`

| 原位置 | 新位置 | 说明 |
|--------|--------|------|
| `output.json` | `output/output.json` | 血缘数据 |
| `index.html` | `output/index.html` | 可视化页面 |

---

## 📝 新建文件

### 1. `README.md`

新的项目主文档，包含：
- 项目简介和特性
- 快速开始指南
- 项目结构说明
- 使用示例
- 测试覆盖
- 数据统计

### 2. `.gitignore`

Git 忽略规则，包含：
- Python 缓存文件（`__pycache__/`, `*.pyc`）
- 虚拟环境（`venv/`, `env/`）
- IDE 配置（`.vscode/`, `.idea/`）
- 输出文件（`output/output.json`, `output/index.html`）
- 测试产物（`.pytest_cache/`, `.coverage`）

### 3. `docs/文件整理说明.md`

本文档，记录整理过程和目录结构。

---

## 🎯 整理目标

### ✅ 已实现

1. **清晰的目录结构** - 按功能分类（文档、测试、示例、输出）
2. **易于导航** - 新用户可以快速找到需要的文件
3. **规范的命名** - 统一的文件命名规范
4. **完整的文档** - 每个目录都有明确的用途说明
5. **Git 友好** - 添加 `.gitignore`，避免提交不必要的文件

### 📌 保持不变

1. **核心模块** - 保留在根目录，便于导入
2. **前端资源** - `app.js`, `vendor.js`, `example.png` 保留在根目录
3. **dbt 配置** - `dbt_project.yml`, `run.sh` 保留在根目录
4. **集成测试** - `integration_tests/` 目录保持不变

---

## 🚀 使用建议

### 对于新用户

1. 先阅读 `README.md` 了解项目概况
2. 阅读 `docs/血缘分析使用指南.md` 学习详细用法
3. 运行 `python3 main.py` 生成血缘数据
4. 查看 `examples/` 目录学习更多用法

### 对于开发者

1. 查看 `docs/MIGRATION_SUMMARY.md` 了解技术架构
2. 运行 `tests/run_all_tests.sh` 验证功能
3. 参考 `docs/FIX_REPORT.md` 了解已知问题和解决方案
4. 查看 `docs/OUTPUT_JSON_GUIDE.md` 了解数据结构

---

## 📊 文件统计

### 整理前

- 根目录文件: 约 20 个
- 文档文件: 散落在根目录
- 测试文件: 散落在根目录
- 示例文件: 散落在根目录

### 整理后

- 根目录文件: 约 10 个（核心模块 + 配置）
- `docs/` 目录: 7 个文档
- `tests/` 目录: 5 个测试文件
- `examples/` 目录: 2 个示例文件
- `output/` 目录: 2 个输出文件（生成）

---

## ⚠️ 注意事项

### 路径更新

由于文件移动，以下路径需要更新：

1. **测试脚本**:
   ```bash
   # 旧路径
   ./run_all_tests.sh
   
   # 新路径
   cd tests && ./run_all_tests.sh
   ```

2. **示例脚本**:
   ```bash
   # 旧路径
   python3 example_lineage_analysis.py
   
   # 新路径
   python3 examples/example_lineage_analysis.py
   ```

3. **输出文件**:
   ```python
   # 旧路径
   data = json.load(open('output.json'))
   
   # 新路径
   data = json.load(open('output/output.json'))
   ```

### 已更新的文件

以下文件已自动更新路径引用：
- `README.md` - 所有文档链接
- `tests/run_all_tests.sh` - 测试文件路径
- `examples/example_lineage_analysis.py` - 输出文件路径
- `examples/visualize_lineage.py` - 输出文件路径

---

## 🎉 总结

通过这次整理，lineagex 项目的文件结构更加清晰、规范，便于：

✅ 新用户快速上手  
✅ 开发者维护和扩展  
✅ 文档管理和更新  
✅ 版本控制和协作

如有任何问题，请参考 `README.md` 或相关文档。

